# CodeRebuilder · 报名材料

---

## 一、项目概述

**项目名称**：CodeRebuilder

**一句话描述**：AI 驱动的 Vue → React / Vue 3 代码重构桌面助手，内置项目依赖分析与逐文件 AI 迁移流水线，大幅降低大规模前端项目迁移的成本与风险。

**技术栈**：Electron · React · TypeScript · Zustand · SQLite · Monaco Editor · OpenAI 兼容 API

---

## 二、产品定位及价值

### 1、目标用户与场景

| 用户群体 | 典型场景 |
|----------|----------|
| 前端工程师 / 技术负责人 | 将公司存量 Vue 2 项目迁移至 React 或 Vue 3，文件数量多（50～500+），人工迁移耗时数周乃至数月 |
| 独立开发者 | 个人项目技术栈升级，希望借助 AI 快速完成迁移，同时保留对每个文件结果的控制权 |
| 外包 / 交付团队 | 接受客户迁移需求，需要高质量、可追溯的迁移交付物，同时控制人力成本 |

**核心痛点**：
- 大型 Vue 项目迁移时，人工逐文件改写效率极低，容易遗漏依赖关系与循环依赖；
- LLM 直接输出缺乏上下文约束，生成质量参差不齐，难以批量审查；
- 现有工具（vue-to-react、v2r-llm 等）仅提供粗稿，缺乏差异对比、版本历史与人工验收机制。

---

### 2、价值主张

**「先分析、再迁移、有保障」** —— 三段式迁移工作流：

1. **项目分析**：在迁移前用 `dependency-cruiser` + `madge` 自动生成依赖图，识别循环依赖与冗余模块，明确核心模块优先级，降低重构风险；
2. **AI 四步流水线**：按 Template → JSX → Script/Hooks → Style 分步调用 LLM，每步聚焦单一任务，显著提升生成质量；
3. **人工验收闭环**：Monaco Editor 并排 Diff 对比、版本历史快照、逐文件接受 / 拒绝，确保每个迁移结果可控可追溯。

---

### 3、预期收益（量化）

| 收益类型 | 指标 | 现状（纯人工） | 目标（借助本工具） | 提升幅度 |
|----------|------|----------------|--------------------|----------|
| 效率类 | 单文件迁移耗时 | 平均 2～4 小时 | 15～30 分钟（含审查） | ↓ 80～90% |
| 效率类 | 100 文件项目总迁移周期 | 4～8 周 | 1～2 周 | ↓ 60～75% |
| 质量类 | 循环依赖识别率 | 人工审查 ≈ 30% | 自动识别 ≥ 90% | ↑ 200%+ |
| 质量类 | 迁移结果可追溯性 | 无版本记录 | 每文件多版本快照 | 质变 |
| 成本类 | AI 调用费用 | 按量付费（OpenAI） | 优先走免费通道（智谱AI / GitHub Models / 腾讯混元） | ↓ 90%+ |
| 体验类 | 开发者对迁移结果的掌控感 | 低（批量脚本黑盒） | 高（逐文件 Diff + 验收） | 质变 |

---

## 三、AI 方案设计

### 1、核心功能与交互流程

```
用户启动应用
    │
    ▼
【方案选择】选择迁移路径
    Vue 2 → Vue 3 / Vue 2 → React / Vue 3 → React
    配置：TypeScript、状态管理、样式方案
    │
    ▼
【打开项目】导入本地项目目录（SQLite 存储，重启自动恢复）
    │
    ├──► 【项目分析】─────────────────────────────────────────────┐
    │        dependency-cruiser（JSON 报告 + DOT 依赖图）          │
    │        madge（JS/TS 模块依赖 JSON + 可选 SVG 图）            │
    │        内置轻量扫描器（快速 DOT 格式）                        │
    │        → 输出：核心模块 / 边缘模块 / 循环依赖 / violations    │
    │        → 可下载：SVG / DOT / JSON                            │
    │                                                              ◄┘
    └──► 【重构工作台】
             左侧：文件树（按状态分色：待处理 / 进行中 / 完成 / 失败）
             右侧：Monaco Editor Diff（原始 Vue ↔ 生成 TSX）
             │
             ▼
         选择文件 → 选择迁移技能（Prompt 模板）
             │
             ▼
         AI 四步流水线（OpenAI 兼容 API）
             Step 1: Template → JSX 骨架
             Step 2: Script + Hooks 逻辑迁移
             Step 3: Style 方案适配
             Step 4: 拼装成品 TSX
             │
             ▼
         人工审查：接受 / 拒绝 / 重试（附追加说明）
             │
             ▼
         版本历史：多版本快照，可随时回溯对比
```

### 2、AI 能力应用点

| 环节 | AI 应用方式 | 模型选型 |
|------|-------------|----------|
| Template 转 JSX | Prompt 注入组件结构约束 + 目标框架规范 | GLM-4-Flash / GPT-4o-mini / DeepSeek |
| Script/Hooks 迁移 | 上下文携带原始 Options API，输出 Composition API / React Hooks | 同上 |
| Style 适配 | 按方案（CSS Modules / Tailwind / Less）生成对应样式 | 同上 |
| Vuex Store 迁移 | 专属技能 Prompt，整文件单轮迁移至 Pinia / Zustand | 同上 |

**免费 AI 通道**（内置，注册即用）：
- 智谱AI GLM-4-Flash：30B 参数，200K 上下文，完全免费
- GitHub Models：支持 GPT-4o / DeepSeek-R1，GitHub 用户直接使用
- 腾讯混元 hunyuan-lite：1M 上下文，Lite 版免费

### 3、创新点与差异化

| 维度 | 传统方案 | CodeRebuilder |
|------|----------|---------------|
| 迁移前分析 | 无 | dependency-cruiser + madge 依赖图，先理清结构再迁移 |
| AI 调用策略 | 整文件单轮 | 四步分治流水线，每步单一职责，质量更稳定 |
| 结果可控性 | 黑盒批量输出 | 逐文件 Diff + 人工验收 + 版本历史 |
| AI 成本 | 依赖付费 API | 优先免费通道，付费为可选备用 |
| 上下文注入 | 无 | 支持注入项目规范 / 依赖约定 / MCP 分析摘要 |
| 外部工具协同 | 无 | 可选 v2r-llm / vue-to-react 先生成粗稿，再由 LLM 精炼 |

---

## 四、Demo

> 本地运行方式：

```bash
nvm use           # 切换至 Node 22
npm install
npm run dev       # 启动 Electron 桌面应用
```

**演示流程建议**：
1. 选择迁移方案（如 Vue 2 → React + Zustand + CSS Modules）
2. 打开示例 Vue 2 项目目录
3. 进入「项目分析」，查看依赖图与循环依赖报告
4. 切换至「重构工作台」，选择一个 `.vue` 文件，点击「开始重构」
5. AI 四步流水线运行完毕后，查看 Monaco Diff 并点击「接受」

---

## 五、可行性评估

### 技术可行性

| 维度 | 评估 |
|------|------|
| 运行环境 | Electron 跨平台桌面应用（macOS / Windows / Linux），无需服务器，本地运行 |
| AI 接入 | 对接 OpenAI 兼容接口，兼容市面主流模型，免费通道开箱即用 |
| 依赖分析 | `dependency-cruiser` / `madge` 通过 `npx` 按需下载，无需用户手动安装 |
| 数据持久化 | SQLite 本地存储项目元数据与版本历史，无云端依赖 |
| 安全性 | API Key 通过 Electron `safeStorage` 加密存储，不写入代码仓库 |

### 成本可行性

- **AI 调用成本**：优先使用免费通道，普通迁移任务 0 元 API 费用；付费通道作为备选；
- **开发成本**：核心功能已完成开发，处于可运行状态；
- **运维成本**：纯本地桌面应用，无服务器运维负担。

### 推广可行性

- 目标用户群体（前端工程师）庞大，Vue 2 → React / Vue 3 迁移需求持续旺盛；
- 工具链开源友好，可通过 GitHub / 掘金 / 前端社区快速触达目标用户；
- 免费 AI 通道降低了用户的使用门槛，有利于冷启动传播。
